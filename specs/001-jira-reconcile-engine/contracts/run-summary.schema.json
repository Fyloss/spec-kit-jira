{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spec-kit-jira/contracts/run-summary.schema.json",
  "title": "Run Summary (--json form)",
  "description": "The machine-readable form of the structured run summary (NFR-5). The DEFAULT output is human-readable prose; this JSON form is the opt-in --json output. Both ports MUST emit byte-identical JSON for identical inputs (Constitution VI, canonical serialisation per research §11).",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "command", "counts", "exit_code"],
  "properties": {
    "schema_version": { "type": "string", "const": "1.0" },
    "command": {
      "enum": ["config", "reconcile", "mention", "adopt"],
      "description": "`adopt` was added by 003 (see 003/contracts/adoption-plan.schema.json): the label-based adoption command emits this same summary, with an `adoption` block carrying its plan."
    },
    "dry_run": { "type": "boolean", "description": "True when produced by a --dry-run twin; the action set MUST equal the real run's (FR-033)." },
    "counts": {
      "type": "object",
      "additionalProperties": false,
      "required": ["created", "updated", "skipped", "warnings", "errors"],
      "properties": {
        "created": { "type": "integer", "minimum": 0 },
        "updated": { "type": "integer", "minimum": 0 },
        "skipped": { "type": "integer", "minimum": 0 },
        "warnings": { "type": "integer", "minimum": 0 },
        "errors": { "type": "integer", "minimum": 0 }
      }
    },
    "actions": {
      "type": "array",
      "description": "The ordered write-action set of a reconcile run; the --dry-run report equals the real run's set exactly (FR-033). URLs are host-relative — the site host is a coordinate that never appears in output (Constitution IV).",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["method", "url"],
        "properties": {
          "method": { "type": "string" },
          "url": { "type": "string" },
          "body": { "type": "object" }
        }
      }
    },
    "warnings": {
      "type": "array",
      "description": "Human-readable lifecycle-safety warnings (withheld transitions, drift); present only when lifecycle facts were supplied.",
      "items": { "type": "string" }
    },
    "notes": {
      "type": "array",
      "description": "Non-gating informational notes (e.g. open blocking links named per FR-037); present only when lifecycle facts were supplied.",
      "items": { "type": "string" }
    },
    "effects": {
      "type": "object",
      "description": "The three effects of a /speckit.jira.config run, reported SEPARATELY (FR-054).",
      "additionalProperties": false,
      "properties": {
        "discovery": { "$ref": "#/$defs/effect" },
        "hooks": { "$ref": "#/$defs/effect" },
        "readme": { "$ref": "#/$defs/effect" }
      }
    },
    "drift": {
      "type": "array",
      "description": "Named drift warnings per ticket + divergent field (FR-031).",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["ticket", "field", "classification"],
        "properties": {
          "ticket": { "type": "string" },
          "field": { "type": "string" },
          "classification": { "enum": ["mapped", "post-scope", "halted", "unknown"] },
          "message": { "type": "string" }
        }
      }
    },
    "flags": {
      "type": "array",
      "description": "Tickets carrying Jira's Flagged marker; transitions withheld by default (FR-036).",
      "items": { "type": "string" }
    },
    "blockers": {
      "type": "array",
      "description": "Open blocking links named as info, non-gating (FR-037).",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["ticket", "blocked_by"],
        "properties": {
          "ticket": { "type": "string" },
          "blocked_by": { "type": "array", "items": { "type": "string" } }
        }
      }
    },
    "hook_health": {
      "type": "object",
      "description": "Hook health checked and reported on every run (FR-047).",
      "additionalProperties": false,
      "required": ["present", "missing", "disabled"],
      "properties": {
        "present": { "type": "array", "items": { "type": "string" } },
        "missing": { "type": "array", "items": { "type": "string" } },
        "disabled": { "type": "array", "items": { "type": "string" } },
        "repair_hint": { "type": "string", "description": "One-command repair instruction when any hook is missing (FR-047)." }
      }
    },
    "adoption": {
      "type": "object",
      "description": "003 delta — the adoption plan of an `adopt` run: the effective configuration, whether the apply phase ran, the applied bindings with their reason, the refused bindings with their remediation, and the out-of-scope folders. Its full shape is defined by 003/contracts/adoption-plan.schema.json, which this reference deliberately does not duplicate."
    },
    "adopted": {
      "type": "array",
      "description": "003 FR-018 — reported by the FIRST reconcile after an adoption: per adopted ticket, that it was adopted and what the reconcile added to it. The reconcile adds nothing outside the managed panel, so the entry names the panel, never the human prose above it.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["ticket", "action"],
        "properties": {
          "ticket": { "type": "string" },
          "action": { "type": "string" }
        }
      }
    },
    "mutations": {
      "type": "array",
      "description": "Every mentioned-ticket mutation logged (FR-049).",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["ticket", "action"],
        "properties": {
          "ticket": { "type": "string" },
          "action": { "type": "string" }
        }
      }
    },
    "exit_code": {
      "type": "integer",
      "minimum": 0,
      "description": "The documented, monotonically escalating exit code (see cli-contract.md)."
    }
  },
  "$defs": {
    "effect": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status"],
      "properties": {
        "status": { "enum": ["written", "unchanged", "created", "repaired", "refused", "skipped"] },
        "detail": { "type": "string" }
      }
    }
  }
}
