# Committed config for the ambiguous-style scenarios (002 US1). No committed
# style declaration: resolution depends entirely on the API signal / --style.
projects:
  - key: AMBI
    epic_strategy: per_repo
    task_strategy: subtask
routing_default: AMBI
