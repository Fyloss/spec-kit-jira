# Spec Kit CORE template — owned by Spec Kit, overwritten on core upgrade.

The extension MUST NEVER create, modify, or delete anything under
`.specify/templates/` (FR-055, SC-009). This sentinel proves a full config run
leaves it byte-identical.
