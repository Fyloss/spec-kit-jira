#!/usr/bin/env bash
# Spec Kit CORE file — owned by Spec Kit, overwritten on core upgrade. The
# extension MUST NEVER create, modify, or delete anything under .specify/scripts/
# (FR-055, SC-009). This sentinel proves a full config run leaves it byte-identical.
echo "spec-kit core prerequisites (do not touch)"
