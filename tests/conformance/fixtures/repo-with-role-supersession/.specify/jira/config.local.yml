# Machine-owned resolved identifiers (010, T086) — as a prior
# `--issue-type CONSUMER=specification=Epic --issue-type CONSUMER=story=Story`
# run would have recorded them: both roles present with source: operator.
# The rest of the binding (issue_types, required_fields, priorities,
# statuses) is rebuilt fresh from discovery on every config run, so only the
# `roles` block — what the supersession comparison actually reads — needs
# to be seeded here.
resolved_ids:
  CONSUMER:
    roles:
      specification:
        logical_name: "Epic"
        id: "10701"
        hierarchy_level: "1"
        subtask: false
        source: operator
      story:
        logical_name: "Story"
        id: "10704"
        hierarchy_level: "0"
        subtask: false
        source: operator
