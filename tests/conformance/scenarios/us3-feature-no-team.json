{
  "name": "us3-feature-no-team",
  "description": "US3 (FR-017): a two-team catalogue but NO personal selection. The feature command emits {active:false}, exit 0, and performs zero Jira calls — the empty mock call log proves the specify flow is byte-for-byte today's behaviour. Byte-identical across ports.",
  "mock": {
    "projects": { "IJT": "team", "WEX": "team" }
  },
  "fixture": "tests/conformance/fixtures/repo-with-teams-noselect",
  "argv": ["feature", "--json", "invoice export"],
  "env": {
    "JIRA_EMAIL": "user@example.com",
    "JIRA_API_TOKEN": "RAWSECRETXYZ",
    "JIRA_NO_SLEEP": "1"
  }
}
