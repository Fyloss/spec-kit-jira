{
  "name": "us5-plan-on-parent",
  "description": "T091: plan.md's Summary prose is rendered under a named Implementation Plan section on the parent (run 1), and an unchanged plan issues zero writes on a second run (run 2) — byte-identical on both ports (spec FR-026/FR-028). The changed-plan replace-in-place case is proven byte-for-byte cross-port at the plan_apply unit level (test_plan_apply_parent.bats/PlanApply.Parent.Tests.ps1, T090).",
  "mock": { "projects": { "COMP": "company" } },
  "fixture": "tests/conformance/fixtures/repo-with-plan",
  "runs": [
    { "argv": ["reconcile", "--json", "specs/001-reporting/spec.md"] },
    { "argv": ["reconcile", "--json", "specs/001-reporting/spec.md"] }
  ],
  "env": {
    "JIRA_EMAIL": "user@example.com",
    "JIRA_API_TOKEN": "RAWSECRETXYZ",
    "JIRA_NO_SLEEP": "1",
    "SPEC_KIT_JIRA_REPO": "acme/app",
    "SPEC_KIT_JIRA_SPEC_SLUG": "001-reporting",
    "SPEC_KIT_JIRA_ID_SOURCE": "aaaaaaaaaaaaaaaa 1111111111111111"
  }
}
