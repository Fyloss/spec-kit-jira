{
  "name": "us3-mandatory-field-refusal",
  "description": "T085/T096a: a project whose parent type declares required custom fields the bridge cannot supply refuses cleanly, naming every unsatisfiable field of every written type in one message — no read, no write, byte-identical on both ports (contracts/hierarchy-resolution.md §5). Run 1 is --dry-run, run 2 is the real run: the refusal is PREDICTED, not discovered mid-write (spec FR-025) — both runs refuse identically.",
  "mock": { "projects": { "PM": "company" }, "createmetaFields": { "10101": "parent-mandatory" } },
  "fixture": "tests/conformance/fixtures/repo-with-mandatory-field",
  "runs": [
    { "argv": ["reconcile", "--dry-run", "--json", "specs/001-reporting/spec.md"] },
    { "argv": ["reconcile", "--json", "specs/001-reporting/spec.md"] }
  ],
  "env": {
    "JIRA_EMAIL": "user@example.com",
    "JIRA_API_TOKEN": "RAWSECRETXYZ",
    "JIRA_NO_SLEEP": "1",
    "SPEC_KIT_JIRA_REPO": "acme/app",
    "SPEC_KIT_JIRA_SPEC_SLUG": "001-reporting",
    "SPEC_KIT_JIRA_ID_SOURCE": "aaaaaaaaaaaaaaaa 1111111111111111"
  }
}
