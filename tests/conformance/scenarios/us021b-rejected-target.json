{
  "name": "us021b-rejected-target",
  "description": "T021b [US2, FR-027]: the target guard (User Story 1, contracts/target-guard.md) is evaluated before the state phase, so pointing reconcile at plan.md instead of spec.md behaves exactly as it does today — exit 1, zero requests — even with a plausible run-state document already recorded for this feature directory. Fixture ships a state file that the run must never open.",
  "mock": { "projects": { "COMP": "company" } },
  "fixture": "tests/conformance/fixtures/repo-with-plan-artifact",
  "argv": ["reconcile", "--json", "specs/001-test-page/plan.md"],
  "env": {
    "JIRA_EMAIL": "user@example.com",
    "JIRA_API_TOKEN": "RAWSECRETXYZ",
    "JIRA_NO_SLEEP": "1"
  }
}
