{
  "name": "us3-feature-prose-dry-run",
  "description": "T087 — the default (non---json) feature output renders the feature prose shape, byte-identical across ports: no run-summary nulls (bash), no raw-JSON catch fallback (PowerShell). Dry-run predicts with zero Jira calls, so the bytes are fully deterministic.",
  "mock": {"projects": {"IJT": "team"}},
  "fixture": "tests/conformance/fixtures/repo-with-teams",
  "argv": ["feature", "--dry-run", "invoice export"],
  "env": {
    "JIRA_EMAIL": "user@example.com",
    "JIRA_API_TOKEN": "RAWSECRETXYZ",
    "JIRA_NO_SLEEP": "1",
    "SPEC_KIT_JIRA_PLAN_CONTEXT": "{\"story_type_id\":\"10201\",\"priority_ids\":{\"P1\":\"1\",\"P2\":\"2\",\"P3\":\"3\"},\"estimation_field_id\":null}"
  }
}
