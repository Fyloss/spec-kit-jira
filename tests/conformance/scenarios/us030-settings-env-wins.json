{"name": "us030-settings-env-wins", "description": "030 \u00a71 C1.2/C1.3: config.yml (fixture repo-030-base-url) declares base_url via the @MOCK_BASE_URL@ sentinel, and personal.yml declares an email \u2014 but the environment ALSO supplies an email, and it wins (unchanged by the chokepoint, which only seeds an UNSET variable). SPEC_KIT_JIRA_BASE_URL is left at the harness's own mock-injected value (never blanked), so the run still succeeds. `config` rather than `reconcile`, for the same content-hash reason as us030-settings-from-files.", "mock": {"projects": {"TEST": "company"}}, "fixture": "tests/conformance/fixtures/repo-030-base-url", "argv": ["config", "--json"], "env": {"JIRA_EMAIL": "env-wins@example.com", "JIRA_NO_SLEEP": "1"}}